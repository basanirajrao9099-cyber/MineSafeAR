#This file is generated by updateDaemonJvm
# Pinned to 21: AGP 9.3.2 + Kotlin 2.4.10 do not support JDK 25, which is what
# this machine has installed. Download URLs are omitted deliberately — the
# foojay resolver declared in settings.gradle.kts resolves the JDK instead, so
# a hardcoded per-OS URL list cannot drift out of date.
toolchainVersion=17
